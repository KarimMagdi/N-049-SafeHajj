<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="30dp"
    android:paddingRight="30dp"
    android:background="@drawable/splash_bg"
    tools:context="hajjhackathon.com.team.safehajj.fragment.RegisterProfileFragment">

    <!-- TODO: Update blank fragment layout -->
    <LinearLayout
        android:id="@+id/profileItemsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="130dp"
            android:src="@drawable/logo"
            android:scaleType="fitXY"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="@dimen/space_16dp"
            android:layout_marginTop="60dp"/>

        <EditText
            android:hint="Circle Name or id"
            android:id="@+id/editText_CircleNameOrId"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/styleEditText"
            android:layout_centerInParent="true" />
        <EditText
            android:hint="User Name"
            android:id="@+id/editText_userId"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/styleEditText"
            android:layout_centerInParent="true" />

        <EditText
            android:hint="User Name"
            android:id="@+id/editText_userName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/styleEditText"
            android:layout_centerInParent="true" />
    </LinearLayout>


    <Button
        android:textAllCaps="false"
        android:background="@drawable/round_rect_primary"
        android:textColor="@color/white"
        android:layout_marginTop="30dp"
        android:id="@+id/btn_signUpCircle"
        android:text="Create"
        android:layout_below="@+id/profileItemsContainer"
        android:layout_width="match_parent"
        android:layout_height="55dp" />
</RelativeLayout>
